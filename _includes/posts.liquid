{% for post in site.posts %}
  {% if include.category %}
    {% comment %} check whether the post contains the category {% endcomment %}
    {% if post.categories contains include.category %}
      {% assign print = true %}
    {% else %}
      {% assign print = false %}
    {% endif %}
  {% else %}
    {% assign print = true %}
  {% endif %}
  {% if print %}

### [{{ post.title }}]({{ post.url }})

{{ post.date | date_to_string }}

{{ post.content }}

---

  {% endif %}
{% endfor %}
